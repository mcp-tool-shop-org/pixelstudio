/** Package identity metadata for asset bundles and manifests. */
export interface PackageMetadata {
  packageName: string;
  version: string;
  author: string;
  description: string;
  tags: string[];
}
